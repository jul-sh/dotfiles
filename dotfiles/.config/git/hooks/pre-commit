#!/bin/sh
# Global pre-commit: core.hooksPath shadows each repo's .git/hooks, so
# chain to the repo-local hook if one is installed.
hook="$(git rev-parse --git-dir)/hooks/pre-commit"
if [ -x "$hook" ]; then
    exec "$hook" "$@"
fi
exit 0
